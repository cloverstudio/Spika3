{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: dev",
            "request": "launch",
            "command": "npm run dev-server",
            "cwd": "${workspaceFolder}"
        }
    ]
}